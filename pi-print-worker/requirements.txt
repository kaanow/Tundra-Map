psycopg[binary]==3.2.3
# The maintained fork. It provides the `brother_ql` module, so imports are
# unchanged. This is what the Pi actually runs — the original brother_ql 0.9.4
# that used to be pinned here is unmaintained.
brother-ql-next==0.12.0
Pillow==11.0.0
qrcode[pil]==8.0
pyusb==1.2.1
# mDNS printer discovery (see discover.py).
zeroconf==0.151.1
