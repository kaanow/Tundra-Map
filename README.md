# Tundra-Map

Local working copy authenticates to GitHub as `kaanow` via a per-folder
credential helper (`/usr/local/bin/git-credential-kaanow-gh`). Other repos
on this machine keep their existing (voxelisKW) auth.
